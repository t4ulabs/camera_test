import 'package:flutter/material.dart';

class UploadVideoView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Upload video view"),
      ),
    );
  }
}
